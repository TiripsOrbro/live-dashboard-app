# Dashboard `data/` folder

This folder is only for **dashboard runtime JSON** (audit state and recurrence).

Macromatix Excel files (**Build To JS.xlsx**, **Stock On Hand.xls**, **Stock On Order.xls**) live in the sibling app:

```
../mmx-report-automation/data/
  workbooks/Build To JS.xlsx
  inbox/samples/
```

See [mmx-report-automation/README.md](../mmx-report-automation/README.md).
