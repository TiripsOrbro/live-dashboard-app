# Periodic Safety Inspection (PSI)

Audit definition: periodic safety inspection questions and rotation rules.

- `psiSchema.js` — question definitions
- `psiStore.js` — session data under `tacaudit/data/periodic-safety/`
- `psiReport.js` — PDF report generation

Schedule and lockouts: see `tacaudit/data/audit-recurrence.json`.
